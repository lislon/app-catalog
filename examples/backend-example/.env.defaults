# Default environment variables for backend-example
# These are used if not overridden by .env or .env.local

# Database - PostgreSQL
AC_CORE_DATABASE_URL="postgres://app-catalog:app-catalog@localhost:5532/app-catalog-core"

# Server
NODE_ENV=development
PORT=4001
