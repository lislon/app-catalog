---
'@igstack/app-catalog-frontend-core': patch
---

A sub-resource deep link now shows its selection as a dismissible "Showing: <name>" filter above the table; clearing it restores every row, keeps the row highlighted, and focuses the search box.
