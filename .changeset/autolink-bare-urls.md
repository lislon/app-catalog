---
'@igstack/app-catalog-frontend-core': minor
---

Catalog text now renders as GitHub-flavoured markdown: bare URLs become clickable links (trailing punctuation stays outside), and tables and `~~strikethrough~~` render. A single `~` stays literal so "~5 min" is not struck through.
