---
'@igstack/app-catalog-frontend-core': patch
---

Header version footer (Pipeline #, SHA, Core version, FE build) now shows behind a small info icon popover instead of always-visible text, keeping the header compact. Content and links are unchanged, just revealed on click. Also added a small "by Igor Golovin" attribution line.
