---
'@igstack/app-catalog-frontend-build-vite': patch
---

The bundled PWA manifest defaults still named another app (`short_name: 'EH'`, `description: 'Jump between environments'`), which the previous changeset claimed to have fixed. They now describe App Catalog.
