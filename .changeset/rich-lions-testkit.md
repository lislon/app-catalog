---
'@igstack/app-catalog-test-kit': patch
---

New package: the integration test harness (mock backend, mock network, page
objects) is now published so downstream apps can run the same scenarios against
their own catalog data.
