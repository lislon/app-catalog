---
'@igstack/app-catalog-frontend-core': patch
---

App detail card now shows a collapsed-by-default "Technical information" section, right after Teams, when the entry has an `aiPrompt` and/or `aiMemory` value set. These AI-facing fields were previously not surfaced in the UI at all.
