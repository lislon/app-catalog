---
'@igstack/app-catalog-frontend-core': minor
---

Add optional AWS Account ID column to sub-resources table. Shows `extra.awsAccountId` with copy-to-clipboard when present; degrades to "—" when absent. Account ID also included in search filter.
