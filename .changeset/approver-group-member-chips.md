---
'@igstack/app-catalog-frontend-core': patch
---

Approver groups with no email now show each member as its own clickable, copyable chip instead of one opaque group chip with a plain-text member list.
