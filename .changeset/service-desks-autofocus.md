---
'@igstack/app-catalog-frontend-core': patch
---

Service Desks view: autofocus the search input when the view loads, matching the
Apps view. Switching to the Service Desks tab now places the cursor in the search
box so users can type immediately.
